# repo-intel
Deterministic, offline, read-only repository intelligence exporter. Point --root at any repo to get a sanitized, portable evidence bundle (files, git state, config, delivery/CI, planning docs, product signals) for grounded analysis without re-discovering the repo from scratch.
